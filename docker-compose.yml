version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: vectorlens
      POSTGRES_PASSWORD: vectorlens123
      POSTGRES_DB: vectorlens
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: